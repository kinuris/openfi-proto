export type ClientData = {
      id:  string,
      mac: string,
      credits: number,
      active: boolean
      remainingSeconds: number,
}

export type CodeData = {
      code: string,
      kind: string,
      units: number
    }